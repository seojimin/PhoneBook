//interface. default public, abstract.
interface INIT_MENU {
    //interface's variable public static final. every class can access but can't change the value
    int INPUT=1, SEARCH=2, DELETE=3, EXIT=4;
}
